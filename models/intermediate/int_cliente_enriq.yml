version: 2

models:
  - name: int_cliente_enriq
    description: Tabela de intermediária para enriquecer os dados da dimensão cliente.
    columns:
      - name: PK_CLIENTE
        description: .
        data_tests:
          - unique
          - not_null